import { VehicleGarage } from "@/features/vehicles/VehicleGarage";

export const metadata = {
  title: "Vehicles · Atlas Ascend",
};

export default function VehiclesPage() {
  return <VehicleGarage />;
}
